{
  "name": "crosstalk",
  "version": "3.0.0",
  "description": "Crosstalk — a self-hosting coordination bus for AI coding-agent sessions: cross-machine messaging, presence, a shared work board with a distributed claim-lock, zero-config leader discovery + failover, and a human console. One clone on any node can host the bus or connect to whoever is hosting.",
  "type": "module",
  "bin": {
    "cc-bus": "./cc-bus.mjs"
  },
  "scripts": {
    "bus": "node cc-bus.mjs start",
    "status": "node cc-bus.mjs status",
    "server": "node server/server.mjs",
    "test": "node test/render.test.mjs && node test/db.test.mjs && node test/rest.test.mjs && node test/server.test.mjs && node test/ws.test.mjs && node test/discovery.test.mjs && node test/supervisor.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "express": "^5.1.0",
    "zod": "^3.24.1"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
